export class User {
    first: string;
    last: string;
    phone: string;
    email: string;
    password: string;
    // createdAt: string;
    // updatedAt: string;
    // id: string;
    constructor(
        // first: string
    ) {
// this.first = first;
     }
}