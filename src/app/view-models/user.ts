export class User {
    _id: string;
    first: string;
    last: string;
    phone: string;
    email: string;
    password: string;
    // createdAt: string;
    // updatedAt: string;
    // id: string;
    constructor(
        // first: string
    ) {
// this.first = first;
     }
}